<?php
/**
 * @package BookingManagementPlugin
 */

// If uninstall is not called from WordPress, exit.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// Delete plugin options.
// delete_option( 'your_plugin_option_1' );
// delete_option( 'your_plugin_option_2' );

// Delete plugin tables (if any).
// global $wpdb;
// $wpdb->query( "DROP TABLE IF EXISTS {$wpdb->prefix}your_plugin_table_name" );

// Remove any additional cleanup tasks specific to your plugin.

// Clear any cached data (if applicable).

// Remove any additional files or directories created by your plugin.

// Optionally, you can perform any other cleanup tasks here.
